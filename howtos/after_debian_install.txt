AFTER DEBIAN INSTALL

# login as root, install some essential package: sudo, bc, ssh, git
apt-get install sudo bc ssh git -y

# add valaki to sudo
adduser valaki sudo

logout and login as valaki

# generate a public key (for e.g. github)
{
  eval `ssh-agent -s`
  cd ~/.ssh && ssh-keygen
  cat id_rsa.pub
  #then add to github (on the website)
}

# clone debian_scripts
sudo git clone https://github.com/zsbali/debian_scripts.git

# clone ec2-api-tools
sudo git clone https://github.com/traviscross/ec2-api-tools.git


